#include <stdio.h>
#include <stdlib.h>

int readNumber();

int main()
{
    int array[32] = {0};
    int base, ziffer, i = 0;

    //do
    //{
        scanf("%d", &base);
    //}
    //while(base >= 2 || base < 10);

    while(ziffer!=-1)
    {
        scanf("%d", &ziffer);
        //check
        if(ziffer!=-1)
            array[i] = ziffer;

        i++;
    }

    for(i = 0; )




    return EXIT_SUCCESS;
}
