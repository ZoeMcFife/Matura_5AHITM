<UserControl x:Class="Students.View.StudentEditable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Students.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
        <TextBlock Name="Id"  Text="{Binding Path=StudentId}" Width="100"></TextBlock>
        <TextBox Text="{Binding Path=Name}" Width="100"></TextBox>
        <TextBlock Text="{Binding Path=AssignedClass.ClassName}" Width="100"></TextBlock>
        <Button Click="DeleteStudent_Click">Delete</Button>
    </StackPanel>
</UserControl>
